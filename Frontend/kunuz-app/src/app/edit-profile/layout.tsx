export const metadata = {
  title: "Edit Profile — Kunuz",
  description: "Update your Kunuz profile information.",
};

import React from "react";

export default function EditProfileLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}