
export const metadata = {
    title: "Add Post — Kunuz",
    description: "Share your heritage discovery with the Kunuz community.",
};

import React from "react";

export default function AddPostLayout({ children }) {
    return <>{children}</>;
}

